<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="jp.smartcompany.job.modules.core.mapper.TmgNotificationCheck.TmgNotificationCheckMapper">

    <select id="tmgFCheckNotification" resultType="java.lang.String">
        SELECT  TMG_F_CHECK_NOTIFICATION(
        '${ntfNo}',
        '${custId}',
        '${compId}',
        '${siteId}'
        )
        FROM
            DUAL
    </select>

</mapper>